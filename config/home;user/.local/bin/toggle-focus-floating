#!/bin/fish

set is_floating (hyprctl activewindow -j | jq -r '.floating')

if test "$is_floating" = "true"
  hyprctl dispatch 'hl.dsp.focus({ window = "tiled" })'
else
  hyprctl dispatch 'hl.dsp.focus({ window = "floating" })'
end

#!/usr/bin/fish

set NAME $argv[1]
set LARGE_FONT $argv[2]

set FONT_OPTION
if test "$LARGE_FONT" = "true"
  set FONT_OPTION -o font_size=17
end

uwsm app -- nohup kitty --class=$NAME $FONT_OPTION $NAME > /dev/null 2>&1 &
disown

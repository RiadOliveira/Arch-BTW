#!/bin/fish

set current (hyprctl activeworkspace -j | jq '.id')

if test "$current" -ge 10
  hyprctl dispatch 'hl.dsp.focus({ workspace = 1 })'
else
  set next (math "$current + 1")
  hyprctl dispatch "hl.dsp.focus({ workspace = $next })"
end

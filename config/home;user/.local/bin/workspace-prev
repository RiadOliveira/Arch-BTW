#!/bin/fish

set current (hyprctl activeworkspace -j | jq '.id')

if test "$current" -le 1
  hyprctl dispatch 'hl.dsp.focus({ workspace = 10 })'
else
  set prev (math "$current - 1")
  hyprctl dispatch "hl.dsp.focus({ workspace = $prev })"
end

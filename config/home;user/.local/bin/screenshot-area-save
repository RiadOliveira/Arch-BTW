#!/bin/bash

GEOM=$(slurp) || exit 1
[[ -z "$GEOM" ]] && exit 0

grim -g "$GEOM" ~/Screenshots/area_screenshot_$(date +%Y%m%d_%H%M%S).png && \
notify-send -a "[Screenshot]" -r 8 -t 2000 -h string:fgcolor:"#50c7e3" "Area saved"
